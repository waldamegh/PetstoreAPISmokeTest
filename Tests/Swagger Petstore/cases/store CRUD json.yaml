test: case
name: store CRUD json
description: <insert description of test case>
casedetails:
  vars:
    $id: $genInteger(70,320)
    $petId: $genInteger(32,423)
    $quantity: $genInteger(73,626)
    $shipDate: $genString(8)
    $status: $genString(6)
  defaultCollection: Swagger Petstore
  steps:
  - name: placeOrder json
    requestTimeout: 0
