test: case
name: pet CRUD json
description: <insert description of test case>
casedetails:
  vars:
    $id: $genInteger(93,297)
    $name: $genString(6)
    $status: $genString(5)
  defaultCollection: Swagger Petstore
  steps:
  - name: addPet json
    requestTimeout: 0
  - name: updatePet json
    requestTimeout: 0
